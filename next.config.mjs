/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['server.kunden.ontron.wissen-lernen.eu', 'secure.gravatar.com', 'server.service.ontron.de']
    }
};

export default nextConfig;
