import global from "@/app/constants/global"

const TITLE = 'Ihre Anfragen'

const NO_TICKETS_TASK = 'Sie haben noch keine offenen Fragen'

const QUANTITY_TITLE = 'Anfragen'

const PERIOD_TYPES = global.PERIOD_TYPES

export default {
    TITLE,
    NO_TICKETS_TASK,
    QUANTITY_TITLE,
    PERIOD_TYPES
}